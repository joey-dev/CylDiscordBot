_734‍.x([["Welcome",()=>Welcome]]);class Welcome {
    constructor(client, settings) {
        client.on('guildMemberAdd', member => {
            this.sendWelcomeMessage(member, client, settings);
        })
    }

    sendWelcomeMessage(member, client, settings) {
        console.log('test');
        console.log(member);
        const welcomeChannel = client.channels.cache.get(settings.serverId.welcome.welcome.channel);
        // const welcomeMessage = `Welcome ${member} to the server! Please look through the <#738678232869765170> and assign yourself a role at <#738678463736840223>`;

        const welcomeMessage = settings.general.welcomeMessage.replace('//name//', member);
        welcomeChannel.send(welcomeMessage)
    }
}
