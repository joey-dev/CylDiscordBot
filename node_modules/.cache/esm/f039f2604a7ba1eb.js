_4a7‍.x([["Help",()=>Help]]);class Help {
    constructor(message, settings) {
        this.help(message, settings);
    }

    help(message, settings) {
        console.log('second help');
        message.reply(settings.help)
            .then(() => {
                message.delete();
            });
    }

}
