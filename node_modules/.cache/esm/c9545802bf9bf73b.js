_bf9‍.x([["GeneralAnnouncements",()=>GeneralAnnouncements]]);class GeneralAnnouncements {
    constructor(command, message, args, settings) {
        this.esmImport = require('esm')(module);

        const file = command.split('-', 1)[0];

        switch (file) {
            case 'generalannouncements':
            case 'help':
                this.help(message, settings);
                break;
            case 'create':
                this.create(command, message, args, settings);
                break;
        }
    }

    help(message, settings) {
        const importHelp = this.esmImport('./help');
        new importHelp.Help(message, settings);
    }


    create(command, message, args, settings) {
        const importCreate = this.esmImport('./create');
        new importCreate.Create(command, message, args, settings);
    }
}
