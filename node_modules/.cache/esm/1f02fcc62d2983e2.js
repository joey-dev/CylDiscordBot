_2d2‍.x([["Settings",()=>Settings]]);class Settings {
    settings;

    constructor() {
        const esmImport = require('esm')(module);

        const importDiscord = esmImport('./discord');
        const discordSettings = new importDiscord.Discord()

        const importServerId = esmImport('./serverId');
        const serverIdSettings = new importServerId.ServerId()

        const importGeneral = esmImport('./general');
        const generalSettings = new importGeneral.General()

        const importTest = esmImport('./test');
        const testSettings = new importTest.Test()

        const importSettings = esmImport('./settings');
        const settingsSettings = new importSettings.Settings()

        const importGeneralAnnouncements = esmImport('./generalAnnouncements');
        const generalAnnouncementsSettings = new importGeneralAnnouncements.GeneralAnnouncements()

        const importEvent = esmImport('./event');
        const eventSettings = new importEvent.Event();

        this.settings = {
            'discord': discordSettings.settings,
            'serverId': serverIdSettings.settings,
            'general': generalSettings.settings,
            'test': testSettings.settings,
            'settings': settingsSettings.settings,
            'generalAnnouncements': generalAnnouncementsSettings.settings,
            'event': eventSettings.settings,
        };
    }

    getSettings() {
        return this.settings;
    }
}
