_68b‍.x([["Test",()=>Test]]);class Test {
    constructor(command, message, args, settings) {
        switch (command) {
            case 'online':
                this.isOnline(message, settings);
                break;
        }
    }

    isOnline(message) {
        message.reply(settings.isOnline)
            .then(replyMessage => {
                message.delete();
                setTimeout(() => {
                    replyMessage.delete();
                }, 1000);
            });


    }
}
