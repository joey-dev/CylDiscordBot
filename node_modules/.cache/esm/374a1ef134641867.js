_346‍.x([["Passive",()=>Passive]]);class Passive {
    constructor(client, settings) {
        this.esmImport = require('esm')(module);

        const importRoles = this.esmImport('./roles');
        new importRoles.Roles(client, settings);

        const importWelcome = this.esmImport('./welcome');
        new importWelcome.Welcome(client, settings);

        const importSettings = this.esmImport('./settings');
        new importSettings.Settings(client, settings);

        const importEvent = this.esmImport('./Event');
        new importEvent.Event(client, settings);
    }
}
