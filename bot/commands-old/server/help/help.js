class Help {
    static command(message, args, client) {
        message.reply("\nHi, you can use the following commands:\n -data (this will show you the data of this server)");
    }
}

export default Help
